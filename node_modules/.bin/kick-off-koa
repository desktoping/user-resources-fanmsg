#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../kick-off-koa/kick-off-koa.js" "$@"
  ret=$?
else 
  node  "$basedir/../kick-off-koa/kick-off-koa.js" "$@"
  ret=$?
fi
exit $ret
